# <场景名称>

> 触发词: <触发词1> / <触发词2>
> 路由: `Skill({skill: "danalyzer:recipe", args: "<子配方名>"})`

## 数据源

### <数据源别名1>
- 注册名: `<datasource_name>`
- 条件: <WHERE 条件说明>

```sql
<固定 SQL，不可含用户输入拼接>
```

### <数据源别名2>（可选，单源可删此段）
- 注册名: `<datasource_name>`

```sql
<固定 SQL>
```

## 指标

| code | 名称 | 来源 | 公式 | 格式化 |
|------|------|------|------|--------|
| metric_1 | <名称> | <数据源别名> | `<公式>` | `<python format>` |
| metric_2 | <名称> | <数据源别名> | `<公式>` | `<python format>` |

## 输出

### KPI 卡片

```
metric_1  metric_2  metric_3  metric_4    （一行 N 列，不超过 6 个）
```

### 图表

| # | 类型 | 标题 | 数据 | x | y | 位置 |
|---|------|------|------|---|---|------|
| 1 | <line/pie/bar/scatter> | <标题> | <数据源别名> | <x字段> | <y字段> | row1, col1-2 |
| 2 | <line/pie/bar/scatter> | <标题> | <数据源别名> | <x字段> | <y字段> | row1, col3 |

### 诊断（可选）

```
<条件1>  → "<诊断文字，可用 {metric_code} 变量>"
<条件2>  → "<诊断文字>"
默认      → "<默认诊断>"
```

### 输出文件

```
output/<文件名>.html
```

## 红线

- SQL 不可修改
- 图表类型/维度不可替换
- 不可追加额外查询或数据源
- security 扫描不可跳过
